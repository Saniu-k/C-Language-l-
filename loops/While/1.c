#include <stdio.h>
int main(){
    // while structure
    // intitiation
    // while(condition){
    //     code
    //     increment/decrement(i++)
    // } for "for"loop all of them are in same line
    int i=1;
    while(i<100){
        printf("%d s",i);
        i++;
    }
    return 0;
}