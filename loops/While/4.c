#include <stdio.h>
int main(){
   int i =10;
    while(i=20){
        printf("hello"); //this is also a infinite loop since its not i==20 the 10 will cut off and 20 will replace i vareiable
    }
    return 0;
}